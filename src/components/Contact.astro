---
---
<!-- Hidden static form for Netlify build-time detection -->
<form name="contact" netlify netlify-honeypot="bot-field" hidden>
  <input type="text" name="name" />
  <input type="email" name="email" />
  <input type="text" name="subject" />
  <textarea name="message"></textarea>
</form>

<section class="s-contact" id="contact">
  <div class="s-contact__inner">

    <a href="/" class="s-contact__back">← Every Day Future</a>

    <!-- Left: heading + info -->
    <div class="s-contact__left">
      <h2 class="s-contact__heading" id="contactHeading">Get in touch</h2>
      <p class="s-contact__body t-body-lg" id="contactIntro">Reach out directly with questions, ideas, or to learn more about working with Every Day Future.</p>
      <div class="s-contact__meta" id="contactMeta">
        <div class="s-contact__meta-item">
          <span class="s-contact__meta-label">Direct</span>
          <span class="s-contact__meta-value">
            <a href="mailto:coach@everydayfuture.work">coach@everydayfuture.work</a>
          </span>
        </div>
        <div class="s-contact__meta-item">
          <span class="s-contact__meta-label">Elsewhere</span>
          <span class="s-contact__meta-value">
            <a href="https://instagram.com/everydayfuture" target="_blank" rel="noopener">Instagram</a>
          </span>
        </div>
      </div>
      <!-- Success state: replaces intro + meta, heading swaps to "Thank you" -->
      <p class="s-contact__body t-body-lg" id="contactConfirm" hidden>Thanks so much for your interest and someone from Every Day Future will be in touch shortly.</p>
    </div>

    <!-- Right: form -->
    <div class="s-contact__form-wrap">
      <form class="s-contact__form" id="contactForm" novalidate>
        <input type="hidden" name="form-name" value="contact" />
        <p style="display:none" aria-hidden="true">
          <label>Don't fill this out: <input name="bot-field" tabindex="-1" autocomplete="off" /></label>
        </p>

        <div class="s-contact__field">
          <label class="s-contact__label" for="contactName">Name</label>
          <input
            class="s-contact__input"
            id="contactName"
            name="name"
            type="text"
            autocomplete="name"
            required
            placeholder="Your name"
          />
        </div>

        <div class="s-contact__field">
          <label class="s-contact__label" for="contactEmail">Email</label>
          <input
            class="s-contact__input"
            id="contactEmail"
            name="email"
            type="email"
            autocomplete="email"
            required
            placeholder="you@example.com"
            inputmode="email"
          />
        </div>

        <div class="s-contact__field">
          <label class="s-contact__label" for="contactSubject">Subject</label>
          <input
            class="s-contact__input"
            id="contactSubject"
            name="subject"
            type="text"
            placeholder="What's on your mind?"
          />
        </div>

        <div class="s-contact__field">
          <label class="s-contact__label" for="contactMessage">Message</label>
          <textarea
            class="s-contact__textarea"
            id="contactMessage"
            name="message"
            required
            placeholder="Your message"
            rows="5"
          ></textarea>
        </div>

        <button type="submit" class="s-contact__submit">Send a note</button>
        <p class="s-contact__confirm" id="contactError" role="alert">That didn't send — please try again, or email coach@everydayfuture.work directly.</p>
      </form>
    </div>

  </div>
</section>

<script is:inline>
(function () {
  var form     = document.getElementById('contactForm');
  var formWrap = document.querySelector('.s-contact__form-wrap');
  var heading  = document.getElementById('contactHeading');
  var intro    = document.getElementById('contactIntro');
  var meta     = document.getElementById('contactMeta');
  var confirm  = document.getElementById('contactConfirm');
  var error    = document.getElementById('contactError');
  if (!form) return;
  form.addEventListener('submit', async function (e) {
    e.preventDefault();
    if (error) error.classList.remove('visible');
    var submitBtn = form.querySelector('.s-contact__submit');
    if (submitBtn) submitBtn.disabled = true;
    var data = new URLSearchParams(new FormData(form)).toString();
    var ok = false;
    try {
      var res = await fetch('/', {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: data
      });
      ok = res.ok;
    } catch (err) { ok = false; }
    if (ok) {
      // GA4 lead conversion — fires only on a confirmed Netlify submit
      if (window.gtag) window.gtag('event', 'generate_lead', { form_name: 'contact' });
      // Success state: heading → "Thank you", intro + meta + form replaced by the message
      if (heading) heading.textContent = 'Thank you';
      if (intro) intro.style.display = 'none';
      if (meta) meta.style.display = 'none';
      if (formWrap) formWrap.style.display = 'none';
      if (confirm) confirm.hidden = false;
    } else {
      if (submitBtn) submitBtn.disabled = false;
      if (error) error.classList.add('visible');
    }
  });
})();
</script>
